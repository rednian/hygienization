const CalculatePosition = {
    methods: {
        calcualatedPosition(pos, image){
           let pos_new = {
               x: window.innerWidth,
               y: window.innerHeight
           };
           return {x: 0, y: 0}
        }
    },
}

export default CalculatePosition;